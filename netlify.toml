[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://your-backend-SERVICE_HASH-REGION.a.run.app/api/v1"
  # Netlify 대시보드 → Site settings → Environment variables 에서 실제 Cloud Run 서비스 URL로 설정하세요
